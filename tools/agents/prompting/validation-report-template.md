# Validation Report Template

Validation:

- `<command>`: passed/failed/not run

Manual checks:

- `<check>`: passed/failed/not run

Residual risk:

- `<risk>` because `<reason>`

Packaging:

- Git checkout: yes/no
- Branch:
- Commit:
- PR:
